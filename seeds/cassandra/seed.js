const client = require('./config.js')

// query = `
//   INSERT INTO crossfit_gyms (country_code, state_province, city, gym_name) 
//     VALUES ('USA', 'CA', 'San Francisco', 'San Francisco CrossFit');
// `;
// client.execute(query)
//   .then(result => console.log(result));
